Generates a Sierpinski's Triangle in Python using Matplotlib.

An explanation of how the algorithm works:
    1. Pick a random starting point within a triangle
    2. Pick a random corner of the triangle
    3. Find the midpoint between the two points
    4. Plot a point at that midpoint
    5. Repeat steps 2-4, but instead of picking a random point, use the midpoint